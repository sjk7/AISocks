{
  "buildType": {
    "default": "debug",
    "choices": {
      "debug": {
        "short": "Debug",
        "long": "Enable debug symbols",
        "buildType": "Debug",
        "settings": {
          "BUILD_TESTS": "ON"
        }
      }
    }
  }
}
