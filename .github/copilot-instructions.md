# Copilot Instructions

## Project Guidelines
- For local builds, tests should not run under ASAN; keep BUILD_TESTS enabled and TLS enabled for local test builds. 
- BUILD_TESTS should be ON in presets unless explicitly requested otherwise.