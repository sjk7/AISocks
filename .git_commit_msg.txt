Perf/cleanup: fix HttpResponse 16KB-per-call bug, remove StringBuilder, deduplicate UrlCodec fromHex, misc parser cleanups

- Fix: HttpResponse::header() was unconditionally reserving 16 KB on every call
  before the branch that never used the buffer; moved reserve inside the else
  branch and eliminated the temp allocation via transparent std::map comparator
- HttpResponse now uses std::map<std::less<>> (heterogeneous lookup), matching
  HttpRequest; no-allocation header lookups with string_view keys
- Remove StringBuilder class entirely; all call sites replaced with std::string
  + reserve() + += / .append()
- UrlCodec: extract shared kFromHex lookup table into file-scope anonymous
  namespace, eliminating duplicate identical static lambdas
- HttpFileServer: remove dead ?/# stripping in resolveFilePath (already stripped
  by HttpRequest::parse()); use string_view in hasDotfileComponent_; use stack
  snprintf for ETag; direct string comparison in checkCacheConditions_

All 42 tests pass, all 9 fuzz targets clean (10k runs each).
